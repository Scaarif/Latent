import React from 'react';

const Explore = () => {
  return (
    <div>
      Explore/Search page - has two versions, the parameters version and the map version
    </div>
  )
}

export default Explore;
